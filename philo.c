#include "philo.h"

int	main(int argc, char **argv)
{
	if (argc < 4 || argc > 6)
	{
		printf("%s", ERROR_ARGS);
		printf("%s", ERROR_USAGE);
		printf("%s", ERROR_USAGE1);
		return (0);
	}

}